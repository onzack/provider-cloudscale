---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: customimages.compute.cloudscale.m.crossplane.io
spec:
  group: compute.cloudscale.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - cloudscale
    kind: CustomImage
    listKind: CustomImageList
    plural: customimages
    singular: customimage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'CustomImage is the Schema for the CustomImages API. "cloudscale.ch
          resource"page_title: "cloudscale.ch: cloudscale_custom_image"'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CustomImageSpec defines the desired state of CustomImage
            properties:
              forProvider:
                properties:
                  firmwareType:
                    description: The firmware type that will be used for servers created
                      with the custom image. Options include bios and uefi.
                    type: string
                  importSourceFormat:
                    description: The file format of the image referenced in the import_url.
                      Options include raw, qcow2.
                    type: string
                  importUrl:
                    description: The URL used to download the image.
                    type: string
                  name:
                    description: The human-readable name of the custom image.
                    type: string
                  slug:
                    description: A string identifying the custom image for use within
                      the API.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      Tags allow you to assign custom metadata to resources:
                      tags = {
                      foo = "bar"
                      }

                      Tags are always strings (both keys and values).
                    type: object
                    x-kubernetes-map-type: granular
                  userDataHandling:
                    description: How user_data will be handled when creating a server.
                      Options include pass-through and extend-cloud-config.
                    type: string
                  zoneSlugs:
                    description: Specify the zones in which the custom image will
                      be available. Options include lpg1 and rma1.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  firmwareType:
                    description: The firmware type that will be used for servers created
                      with the custom image. Options include bios and uefi.
                    type: string
                  importSourceFormat:
                    description: The file format of the image referenced in the import_url.
                      Options include raw, qcow2.
                    type: string
                  importUrl:
                    description: The URL used to download the image.
                    type: string
                  name:
                    description: The human-readable name of the custom image.
                    type: string
                  slug:
                    description: A string identifying the custom image for use within
                      the API.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      Tags allow you to assign custom metadata to resources:
                      tags = {
                      foo = "bar"
                      }

                      Tags are always strings (both keys and values).
                    type: object
                    x-kubernetes-map-type: granular
                  userDataHandling:
                    description: How user_data will be handled when creating a server.
                      Options include pass-through and extend-cloud-config.
                    type: string
                  zoneSlugs:
                    description: Specify the zones in which the custom image will
                      be available. Options include lpg1 and rma1.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.importUrl is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.importUrl)
                || (has(self.initProvider) && has(self.initProvider.importUrl))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.userDataHandling is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.userDataHandling)
                || (has(self.initProvider) && has(self.initProvider.userDataHandling))'
            - message: spec.forProvider.zoneSlugs is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.zoneSlugs)
                || (has(self.initProvider) && has(self.initProvider.zoneSlugs))'
          status:
            description: CustomImageStatus defines the observed state of CustomImage.
            properties:
              atProvider:
                properties:
                  checksums:
                    additionalProperties:
                      type: string
                    description: The checksums of the custom image as map.
                    type: object
                    x-kubernetes-map-type: granular
                  firmwareType:
                    description: The firmware type that will be used for servers created
                      with the custom image. Options include bios and uefi.
                    type: string
                  href:
                    description: The cloudscale.ch API URL of the current resource.
                    type: string
                  id:
                    type: string
                  importHref:
                    description: The cloudscale.ch API URL of the custom image import.
                    type: string
                  importSourceFormat:
                    description: The file format of the image referenced in the import_url.
                      Options include raw, qcow2.
                    type: string
                  importStatus:
                    description: The status of the custom image import. Options include
                      started, in_progress, failed, success.
                    type: string
                  importUrl:
                    description: The URL used to download the image.
                    type: string
                  importUuid:
                    description: The UUID of the custom image import.
                    type: string
                  name:
                    description: The human-readable name of the custom image.
                    type: string
                  sizeGb:
                    description: The size in GB of the custom image.
                    type: number
                  slug:
                    description: A string identifying the custom image for use within
                      the API.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      Tags allow you to assign custom metadata to resources:
                      tags = {
                      foo = "bar"
                      }

                      Tags are always strings (both keys and values).
                    type: object
                    x-kubernetes-map-type: granular
                  userDataHandling:
                    description: How user_data will be handled when creating a server.
                      Options include pass-through and extend-cloud-config.
                    type: string
                  zoneSlugs:
                    description: Specify the zones in which the custom image will
                      be available. Options include lpg1 and rma1.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
